@model IEnumerable<BiasedSocialMedia.Web.Models.Notifications>
@{ foreach (BiasedSocialMedia.Web.Models.Notifications notification in Model)
    {
        Html.RenderPartial("_SingleNotification", notification);
    }
}